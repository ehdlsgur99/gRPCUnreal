# Visual Studio 2015 user specific files
gRPC/.vs/

# Compiled Object files
gRPC/*.slo
gRPC/*.lo
gRPC/*.o
gRPC/*.obj

# Precompiled Headers
gRPC/*.gch
gRPC/*.pch

# Compiled Dynamic libraries
gRPC/*.so
gRPC/*.dylib
gRPC/*.dll

# Fortran module files
gRPC/*.mod

# Compiled Static libraries
gRPC/*.lai
gRPC/*.la
gRPC/*.a
gRPC/*.lib

# Executables
gRPC/*.exe
gRPC/*.out
gRPC/*.app
gRPC/*.ipa

# These project files can be generated by the engine
gRPC/*.xcodeproj
gRPC/*.xcworkspace
gRPC/*.sln
gRPC/*.suo
gRPC/*.opensdf
gRPC/*.sdf
gRPC/*.VC.db
gRPC/*.VC.opendb

# Precompiled Assets
gRPC/SourceArt/**/*.png
gRPC/SourceArt/**/*.tga

# Binary Files
gRPC/Binaries/*
gRPC/Plugins/**/Binaries/*

# Builds
gRPC/Build/*

# Whitelist PakBlacklist-<BuildConfiguration>.txt files
gRPC/!Build/*/
gRPC/Build/*/**
gRPC/!Build/*/PakBlacklist*.txt

# Don't ignore icon files in Build
gRPC/!Build/**/*.ico

# Built data for maps
gRPC/*_BuiltData.uasset

# Configuration files generated by the Editor
gRPC/Saved/*

# Compiled source files for the engine to use
gRPC/Intermediate/*
gRPC/Plugins/**/Intermediate/*

# Cache files for the editor to use
gRPC/DerivedDataCache/*